[project]
name = "mongo-db"
version = "0.1.0"
description = ""
authors = [
    {name = "mayconct32",email = "maycon326754@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pymongo (>=4.16.0,<5.0.0)"
]

[tool.poetry]
packages = [{include = "mongo_db", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
